using Ming.Engine;

namespace Ming.Managers
{
    public class MingLevelManager : MingBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
